import type {
  TFontSize,
  TMaxOptionItems,
  TClearable,
  TDisabled,
} from './my-select.types';

const DEFAULT_SELECTED_FONT_SIZE: TFontSize = 18;
const DEFAULT_OPTIONS_FONT_SIZE: TFontSize = 16;
const DEFAULT_MAX_OPTION_ITEMS: TMaxOptionItems = 5;
const DEFAULT_CLEARABLE: TClearable = false;
const DEFAULT_DISABLED: TDisabled = false;

export default {
  DEFAULT_SELECTED_FONT_SIZE,
  DEFAULT_OPTIONS_FONT_SIZE,
  DEFAULT_MAX_OPTION_ITEMS,
  DEFAULT_CLEARABLE,
  DEFAULT_DISABLED,
};
