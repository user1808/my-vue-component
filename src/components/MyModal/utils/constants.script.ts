import type {
  THideTitle,
  TResizable,
  TStayOpened,
  TType,
  TBottomTypeScrollable,
} from './my-modal.types';

const DEFAULT_HIDE_TITLE: THideTitle = false;
const DEFAULT_RESIZABLE: TResizable = false;
const DEFAULT_STAY_OPENED: TStayOpened = false;
const DEFAULT_TYPE: TType = 'central';
const DEFAULT_BTM_TYPE_SCROLLABLE: TBottomTypeScrollable = false;

export default {
  DEFAULT_HIDE_TITLE,
  DEFAULT_RESIZABLE,
  DEFAULT_STAY_OPENED,
  DEFAULT_TYPE,
  DEFAULT_BTM_TYPE_SCROLLABLE,
};
